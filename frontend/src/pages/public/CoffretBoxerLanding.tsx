/**
 * Stub temporaire — fichier original perdu (0 bytes en local).
 * Bascule vers DynamicLanding qui fetch la config depuis l'API.
 * TODO: reconstruire le design custom luxe quand on aura un creneau.
 */
import DynamicLanding from './DynamicLanding';

export default function CoffretBoxerLanding() {
  return <DynamicLanding />;
}
