# Sauvegarde production - 2026-04-18 21:48

État de la prod au moment du snapshot : **stable, testé, déployé**.
- 7 slugs servis par le VPS (obrille.com / coachingexpertci.com)
- obgestion.com fermé au public (14 redirects 308 vers obrille.com)
- Back-office obgestion.com intact
- Proxy PHP same-origin actif sur le VPS

---

## Références de l'état sauvegardé

### Git
- **Commit** : `395da2bc` (`sauvegarde: fermeture obgestion au public + 7 slugs sur VPS + formulaires custom`)
- **Tag** : `backup-2026-04-18-prod` (poussé sur origin)
- **Branche** : `main`

### Vercel (frontend obgestion.com)
- **Project** : `gs-pipeline-app` (nandees-projects-5d9c6125)
- **Deployment URL** : `https://gs-pipeline-qq15skzat-nandees-projects-5d9c6125.vercel.app`
- **Inspect** : `https://vercel.com/nandees-projects-5d9c6125/gs-pipeline-app/3LecaHT4jgfeZk3JCpt8MHW2CYG2`
- **Aliasé sur** : `obgestion.com`

### VPS (obrille.com / coachingexpertci.com)
- **Host** : `vps112526.serveur-vps.net`
- **User** : `obrilleshell`
- **Archive locale** : `backups/vps-prod-backup-20260418-214800.tar.gz` (2.43 MB)
- **Archive distante** : `/tmp/vps-prod-backup-20260418-214800.tar.gz`
- **Contenu** : `.htaccess`, `landings-app/`, `api/` (proxy PHP)

---

## Procedures de restauration

### 1. Restaurer le code (rollback git)

```bash
git checkout backup-2026-04-18-prod
# ou pour reset hard sur main :
git reset --hard backup-2026-04-18-prod
git push --force origin main  # ATTENTION : destructif
```

### 2. Restaurer Vercel (obgestion.com)

Option A — promote l'ancien deployment :
```bash
npx vercel promote https://gs-pipeline-qq15skzat-nandees-projects-5d9c6125.vercel.app --scope nandees-projects-5d9c6125
```

Option B — redeploy depuis le tag :
```bash
git checkout backup-2026-04-18-prod
# temporairement linker au projet frontend
cp .vercel/project.json .vercel/project.backend.bak
echo '{"projectId":"prj_0JWZCVArRAjd8BN3nYJJdWHPlEfT","orgId":"team_jikj9ZfzNh0qSfEhJdnk3atm","projectName":"gs-pipeline-app"}' > .vercel/project.json
npx vercel --prod --yes
mv .vercel/project.backend.bak .vercel/project.json
```

### 3. Restaurer le VPS

#### Restaurer .htaccess uniquement (rapide)
```bash
ssh -i ~/.ssh/obrille_temp obrilleshell@vps112526.serveur-vps.net 'ls -lt /web/.htaccess.bak-* | head -5'
# choisir un backup (par exemple .htaccess.bak-pre-7slugs-20260418-212630)
ssh -i ~/.ssh/obrille_temp obrilleshell@vps112526.serveur-vps.net 'cp /web/.htaccess.bak-pre-7slugs-20260418-212630 /web/.htaccess'
```

#### Restaurer tout (.htaccess + landings-app + api) depuis l'archive
```bash
# Si l'archive distante existe encore :
ssh -i ~/.ssh/obrille_temp obrilleshell@vps112526.serveur-vps.net 'cd /web && tar -xzf /tmp/vps-prod-backup-20260418-214800.tar.gz'

# Sinon, ré-uploader depuis le local :
node scripts/push-vps-file.mjs backups/vps-prod-backup-20260418-214800.tar.gz /tmp/restore.tar.gz
ssh -i ~/.ssh/obrille_temp obrilleshell@vps112526.serveur-vps.net 'cd /web && tar -xzf /tmp/restore.tar.gz'
```

#### Redeploy du bundle React depuis git
```bash
git checkout backup-2026-04-18-prod
node scripts/deploy-vps.mjs
```

---

## Backups internes du VPS (auto)

Le VPS conserve automatiquement plusieurs `.htaccess.bak-*` dans `/web/` :
- `/web/.htaccess.bak-pre-7slugs-20260418-212630` (avant ajout des 2 slugs)
- `/web/.htaccess.before-react-20260418-182354` (avant la migration React)
- `/web/.htaccess.bk` (très ancien, ~5 sept 2025)

Le script `deploy-vps.mjs` conserve aussi les 3 derniers backups d'`assets/` dans `/web/landings-app/` :
- `assets.bak-YYYYMMDD-HHMMSS`

---

## Tests de validation post-restauration

```powershell
# 7 slugs servis (200 React)
$slugs = @('creme-anti-verrue','creme-verrue-tk','spraydouleurtk','creme-ongle-incarne','chaussette-compression','patchdouleurtk','crememinceurfb')
foreach ($s in $slugs) {
  $code = curl.exe -s -o NUL -w "%{http_code}" "https://obrille.com/$s"
  Write-Host "  $s -> $code"
}

# Redirects 308 sur obgestion
foreach ($s in $slugs) {
  $r = curl.exe -s -o NUL -w "%{http_code}|%{redirect_url}" "https://obgestion.com/$s"
  Write-Host "  /$s -> $r"
}

# Back-office intact
curl.exe -s -o NUL -w "%{http_code}\n" "https://obgestion.com/login"
curl.exe -s -o NUL -w "%{http_code}\n" "https://obgestion.com/admin"
```
